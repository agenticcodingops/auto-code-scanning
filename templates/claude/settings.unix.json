{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "UNIX (macOS/Linux) variant — uses bash so no pwsh is required. setup-scan-fix installs THIS file as .claude/settings.json on non-Windows hosts; the pwsh variant (settings.json) is used on Windows. Same in-session scan->self-correct behaviour either way.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/posttooluse-scan.sh",
            "timeout": 120
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/stop-scan.sh",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
